[pytest]
markers =
    auth: Authentication and Authorization tests
    injection: SQLi, XSS, and payload injection tests
    api: API security and mass assignment tests
    destructive: Tests that modify state (must be carefully isolated)
addopts = --strict-markers -v
testpaths = .
